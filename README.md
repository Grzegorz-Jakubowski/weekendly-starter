# Weekendly

Starter for the React course. Requires Node.js 24 (24.15 or newer).

```sh
npm install
```

The app needs two terminals: one for the places API, one for Vite.

```sh
npm run api
```

```sh
npm run dev
```

```sh
npm run check
npm run storybook
```
